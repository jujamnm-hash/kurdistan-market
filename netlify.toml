[build]
  base = "frontend"
  publish = "build"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"

# React Router fix - redirect all to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
